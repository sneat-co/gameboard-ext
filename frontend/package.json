{
  "name": "@sneat/extension-gameboard-contract",
  "version": "0.1.0",
  "description": "Published TS contract for the gameboard extension: wire types + the deterministic event-timeline fold reducer, hand-implemented to match ../typespec/api4gameboard.tsp and proven (parity.json) to agree with the Go reducer in gameboard-ext/backend/eventtimeline.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "node node_modules/vitest/vitest.mjs run",
    "typecheck": "node node_modules/typescript/bin/tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  }
}
